<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 유입 폼을 허용할 랜딩페이지 호스트
    |--------------------------------------------------------------------------
    |
    | POST /crm/member 처리 후 사용자를 되돌려 보낼 수 있는 호스트 목록이다.
    | 여기에 없는 호스트로는 리다이렉트하지 않는다 (Open Redirect 방지).
    | 서브도메인은 자동으로 포함된다. 예: example.com → ad.example.com 허용
    |
    */

    'allowed_hosts' => array_filter(
        array_map('trim', explode(',', env('LANDING_ALLOWED_HOSTS', '')))
    ),

];
